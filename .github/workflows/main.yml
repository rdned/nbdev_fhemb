name: Runner test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted
    steps:
      - name: Print hostname
        run: hostname

      - name: Check psql version
        run: psql --version || echo "psql not found"

      - name: Check docker
        run: docker ps || echo "docker not available"
